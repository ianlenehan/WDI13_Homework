var bank = {
  var accounts = []
  var newAccount= {name:, number:, amount:}

  var addAccount = function(money){
    var total = 0;
    for(i = 0; i < amount.length; i++)
    total += amount[i].bank;
  }

var returnTotal = function(money){
  var total = 0;
  for(i = 0; i < amount.length; i++)
  total += amount[i].bank;
  return total;
}
returnTotal(bank.accounts);
